#using dictionaries
#a dictionary is a collection which is unordered, changeable and indexed. no duplicate memebers

#create dictionary
person = {
    'first key': 'first value',
    'second key': 'second value'
}

# iterator = iter(person)
# print(next(iterator))
# print(next(iterator))


# import first as firstModule
# from first import name

# print (name)

import platform

print( dir(platform.machine))
